# Existing-repository AI transfer evidence

Status: GENERATED/EVIDENCE
Date: 2026-08-23
Foundation ruleset: 1.2.0

## Result

AI-assisted transfer of the Foundation into existing repositories has been performed successfully in multiple real repositories. This validates the practical existing-repository transfer/integration path beyond synthetic installer tests.

Successful target repositories reported by the user:

- `gecompat/FolioTone`
- `gecompat/SQL_Server_Lab`
- `gecompat/SQL_Server_Toolbelt`
- `gecompat/SQL_Server_Analyze`
- `gecompat/SQL_PerformanceSchulung`

## Validated scope

This evidence validates that an AI can use the Foundation transfer/integration contract to apply the Foundation to existing repositories while preserving target-project governance as a separate semantic layer.

The successful transfers provide manual/empirical evidence for:

- existing-repository AI transfer is practically usable;
- the Foundation can be integrated into mature repositories with existing governance;
- target-project rules remain part of the integration instead of being replaced wholesale;
- semantic integration is a real supported use case, not only a synthetic design assumption.

## Scope boundary

This evidence does **not by itself** prove the complete `MV-FND-001` continuation criterion that a genuinely fresh AI, after transfer and with no prior conversation context, can correctly explain and continue every target project using repository state alone.

Therefore:

- existing-repository AI transfer: `validated`;
- fresh-agent post-transfer continuation: `pending manual validation` until separately evidenced.

See `Documentation/Quality/MANUAL_VALIDATION_FRESH_AI_TRANSFER.md` for the remaining continuation acceptance contract.
