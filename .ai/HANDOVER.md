# Handover

Status: GENERATED/EVIDENCE

## Current state

The v1.2 candidate extends rules-only transfer with a formal semantic-integration contract for existing repositories. File states remain deterministic (`CREATE`, `UNCHANGED`, `MERGE_REQUIRED`, `CONFLICT`), while meaningful governance overlaps are classified separately so mature target rules are preserved rather than normalized to Foundation wording.

Active project governance must remain discoverable from root `AGENTS.md`. Foundation `REQUIRED` rules are minimum floors; `PROJECT_STRONGER`, `PROJECT_SELECTABLE_OVERRIDE`, and `COMPLEMENTARY` target rules are preserved. Unique adapter governance must be rehomed before adapters are thinned. Richer project validation-status vocabularies and model-routing policies remain valid through semantic mapping.

Foundation CI run `32646967820` validated the implementation head `17662ba58a88abee8ef951d22918aa4c5543392d`. FND-005, FND-008, and FND-009 are therefore deterministically complete. Foundation validation remains `FOUNDATION_INTEGRITY` only; target `PROJECT_SEMANTIC`/`RUNTIME_EMPIRICAL` responsibilities are unchanged.

## Next actions

1. Confirm Foundation CI on the final evidence/PR head.
2. Merge the v1.2 semantic-integration PR only after that final CI result is green.
3. Execute `Documentation/Quality/MANUAL_VALIDATION_FRESH_AI_TRANSFER.md` with a genuinely fresh AI session/test target when available; do not claim it as executed beforehand.
4. Complete FND-001 only after that manual acceptance is recorded.

## Open constraints

- The deterministic installer does not perform semantic merges; the AI protocol owns semantic classification for existing repositories.
- The Foundation validator can verify the integration contract is installed but cannot deterministically prove that every target-specific active rule was correctly inventoried, discovered, or classified; that remains `PROJECT_SEMANTIC` review.
- Manifest hashes/cross-version installed provenance remain pending under FND-002.
- Vendor adapter discovery behavior can change and must be rechecked against current primary documentation when adapters change.
